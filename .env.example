# Strapi REST API base URL (server-side only).
# Local dev: http://localhost:1337/api
STRAPI_API_BASE_URL=https://your-strapi-host.example.com/api

# Shared secret used by Strapi webhook → /api/revalidate
REVALIDATION_SECRET=change-me
